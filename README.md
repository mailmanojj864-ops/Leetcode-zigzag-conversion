# Zigzag String Conversion (C)

## 📌 Overview
This repository contains a **C implementation** of the **Zigzag String Conversion** problem from **LeetCode**.

The program converts a given string into a zigzag pattern based on a specified number of rows and then reads the pattern row by row to produce the final string.

This solution is:
- Fully compatible with LeetCode
- Efficient and easy to understand
- Written using standard C libraries

  ⏱️ Time Complexity:
  O(n)

  💾 Space Complexity:
  O(n)
